# RECOVERI — Универсальный советник для MT5

Советник для **«разруливания» счёта из минуса**: берёт под управление существующие позиции (свои, ручные или все подряд) и закрывает их корзиной по выбранной стратегии восстановления.

**Версия 1.48** — Mode 5 (Partial Recovery): **общий ТП теперь сам откусывает чип от глубочайшего убыточника**.

Фикс продолжения проблемы из v1.47. До 1.46 убыточник «съедался» в `ProcessProfitableAveragers`: каждый отдельный `PR-AVG-*` при срабатывании своего `InpAvgTPpts` закрывался И откусывал `InpPartCloseLot` от худшего убыточника / `PR-LOCK`. v1.47 (правильно) **приостановила** этот путь, как только цепочка дорастает до `InpPRCommonTPCount`, чтобы общий ТП успевал срабатывать. Но сама `CheckPRCommonTP` тогда только закрывала цепочку и клала реализованный профит на баланс — исходный убыточник не сокращался, и каждый следующий цикл «закрылась корзина → открылась новая» возвращал ту же убыточную позицию.

В 1.48 после **каждого** срабатывания общего ТП (денежного `InpPRCommonTPMoney` или ценового `InpPRCommonTPPts`, на сторону) советник теперь:

1. Суммирует реализованный профит **только тех** `PR-AVG`, которые он действительно закрыл в этом проходе (`Profit+Swap+Commission` на момент закрытия; failed-close автоматически игнорируется).
2. Откусывает **один большой чип** от самого глубокого исходного убыточника лотом, подобранным так, чтобы реализованная пара (профит цепочки + убыток на чипе) осталась `>= InpMinNetProfit` при `InpEnsureNetPositive=true`.

Логика выбора стороны убыточника:

- **Ценовой ТП BUY-цепочки** (`Bid >= WAvgBuy + InpPRCommonTPPts*Point`) → чип со стороны **SELL-убыточников** (BUY зарабатывал, потому что цена шла вверх → SELL терпел).
- **Ценовой ТП SELL-цепочки** (`Ask <= WAvgSell - InpPRCommonTPPts*Point`) → чип со стороны **BUY-убыточников**.
- **Денежный ТП** (закрывает обе стороны) → чип от самого глубокого убыточника **на любой стороне**.

Если убыточников больше нет / минимальный лот не помещается в бюджет — чип молча пропускается и реализованный профит просто остаётся на балансе (та же конвенция, что в `ProcessProfitableAveragers`). Логи: `PR common-TP chipped X.XX from #ticket (budget=Y.YY, prio=N, mode=net+/fixed)` или `PR common-TP chip skipped — <причина>`.

Управляется новым ключом **`InpPRCommonTPChipLoser`** (по умолчанию `true`). Установка `false` возвращает v1.47-поведение, когда общий ТП только закрывает цепочку и не трогает убыточник. `InpEnsureNetPositive` / `InpMinNetProfit` / `InpPartCloseLot` применяются единообразно с per-averager-чипом.

| Параметр | Описание | Дефолт |
|---|---|---|
| `InpPRCommonTPChipLoser` | После общего ТП откусывать чип от глубочайшего убыточника на реализованный профит (`false`=только закрыть цепочку, v1.47 поведение) | `true` |

**Версия 1.47** — Mode 5 (Partial Recovery): **общий ТП теперь имеет приоритет над per-averager TP+chip**.

Фикс поведения, которое критиковал пользователь: «Mode 5 неправильно строит сетку — каждый усреднитель закрывается сам по себе откусыванием лока, и общий ТП на 5 усреднений никогда не активируется».

До 1.46 при включённом общем ТП в Mode 5 советник одновременно крутил две схемы закрытия:

- `ProcessProfitableAveragers` — закрывал каждый отдельный `PR-AVG-*`, как только тот набирал `InpAvgTPpts` пунктов, плюс откусывал чип `InpPartCloseLot` от худшего убыточника / лок-ноги (`PR-LOCK`);
- `CheckPRCommonTP` — должен был закрыть всю цепочку, когда в ней набирается `InpPRCommonTPCount` ордеров.

Из-за per-position TP цепочка обычно не успевала дорасти до порога: усреднители разбирались поодиночке, общий ТП висел армированным, но никогда не срабатывал.

В 1.47 поведение изменено: **как только у советника одновременно открыто `>= InpPRCommonTPCount` усреднителей** (и общий ТП в принципе включён через `InpPRCommonTPPts > 0` или `InpPRCommonTPMoney > 0`), per-position TP+chip и closing overlap автоматически **приостанавливаются**. Цепочка управляется только общим ТП (`Bid >= WAvgBuy + InpPRCommonTPPts*Point`, `Ask <= WAvgSell - InpPRCommonTPPts*Point`, или денежный триггер `InpPRCommonTPMoney`). Когда общий ТП закроет корзину и счётчик упадёт ниже порога, per-position TP+chip и overlap автоматически возобновятся для следующего цикла.

Управляется новым ключом **`InpPRCommonTPSuspendsAvgTP`** (по умолчанию `true`). Установка `false` возвращает старое v1.46 поведение (две схемы работают параллельно).

Расчёт открытых усреднителей идёт по живой таблице позиций (`CountPRAveragers`), а не по счётчикам `g_prAvgCount{Buy,Sell}` — счётчики могут дрейфовать после ручных закрытий, смены режимов или перезагрузки persistence-state. Лог `PR: chain reached X/Y -> per-avg TP+chip and overlap SUSPENDED, common TP active` появляется один раз на изменение количества открытых усреднителей, чтобы было видно момент переключения.

**Версия 1.46** — Mode 5 (Partial Recovery): **общий ТП цепочки усреднителей после N открытых ордеров**.

В v1.45 общий ТП появился у безусловной сетки `InpUseUncondGrid`. Теперь та же схема работает и в Mode 5 для цепочки `PR-AVG-*`. До 1.46 каждый усреднитель ждал свой `InpAvgTPpts`, и при глубокой просадке цепочка могла зависнуть надолго даже при общем плюсе. Теперь, как только у советника одновременно открыто **`InpPRCommonTPCount`** усреднителей (BUY+SELL вместе, по умолчанию 5), активируется общий ТП на корзину `PR-AVG-*`:

- **Ценовой ТП (на сторону).** Считается средневзвешенная цена открытия отдельно по `PR-AVG-B` и по `PR-AVG-S`. Когда `Bid >= WAvgBuy + InpPRCommonTPPts*Point` — закрываются все BUY-усреднители разом. Когда `Ask <= WAvgSell - InpPRCommonTPPts*Point` — аналогично закрываются все SELL-усреднители. По умолчанию `InpPRCommonTPPts = 50`. Установка `0` отключает ценовой ТП.
- **Денежный ТП (на всю цепочку).** Когда сумма PnL всех усреднителей (BUY + SELL, с учётом swap+commission) пересекает порог `InpPRCommonTPMoney` (валюта депо), закрывается вся цепочка одним проходом. По умолчанию `0` (выкл).

Идентификация позиций цепочки строгая: `Magic == InpMagic` **И** комментарий содержит подстроку `"PR-AVG"` (это покрывает обе стороны `PR-AVG-B/S`). Лок-нога (`PR-LOCK`), исходные убыточные позиции, любые позиции из других режимов восстановления (`AVG-`, `GRID-`, `MANUAL-LOCK`) — **не трогаются**. Декремент счётчиков `g_prAvgCount*` происходит автоматически на каждом закрытии, поэтому новая цепочка после флипа тренда / следующего цикла стартует с мультипликатором от нуля — это поведение совместимо с `InpRestartGridOnTrendFlip` и `TryCloseOldGridByProfit`.

Параметры группы «Partial Recovery (Mode 5)»:

| Параметр | Описание | Дефолт |
|---|---|---|
| `InpPRCommonTPCount` | Кол-во активных PR-AVG (обе стороны вместе) для активации общего ТП (`0`=выкл) | `5` |
| `InpPRCommonTPPts` | Общий ТП от средневзв. цены цепочки в пунктах, **на сторону** (`0`=выкл) | `50` |
| `InpPRCommonTPMoney` | Общий ТП по сумме PnL всей цепочки в валюте депо (`0`=выкл) | `0.0` |
| `InpPRCommonTPSuspendsAvgTP` | При достижении порога приостанавливать per-avg TP+chip и overlap (v1.47; `false`=v1.46 параллельно) | `true` |

Денежный триггер имеет приоритет: если суммарный PnL ≥ `InpPRCommonTPMoney`, закрывается **вся цепочка** независимо от цены. Если денежный триггер не сработал — независимо проверяются ценовые ТП по сторонам. **С v1.47** при `InpPRCommonTPSuspendsAvgTP = true` (дефолт) после достижения порога per-position TP усреднителей (`InpAvgTPpts`) и closing overlap приостанавливаются — цепочка ведётся только общим ТП. Установив `false`, можно вернуть v1.46-поведение, когда обе схемы работают параллельно.

**Версия 1.45** — Безусловная сетка: **общий ТП после N сработавших ордеров**.

В режиме `InpUseUncondGrid` каждый сработавший лимитник раньше жил «сам по себе» — закрывался либо общим триггером корзины, либо своими виртуальными TP/SL/TSL. Теперь, как только в сетке набирается **`InpGridCommonTPCount`** (по умолчанию 5) сработавших ордеров, советник активирует общий ТП на всю сеточную корзину:

- **Ценовой ТП (на сторону).** Считается средневзвешенная цена открытия отдельно по BUY-сеточным позициям и по SELL-сеточным позициям. Когда `Bid >= WAvgBuy + InpGridCommonTPPts*Point`, все BUY-сеточные позиции закрываются разом. Когда `Ask <= WAvgSell - InpGridCommonTPPts*Point`, аналогично закрываются все SELL-сеточные позиции. По умолчанию `InpGridCommonTPPts = 50`. Установка `0` отключает ценовой ТП.
- **Денежный ТП (на всю корзину сетки).** Когда сумма PnL всех сеточных позиций (BUY + SELL, с учётом swap+commission) пересекает порог `InpGridCommonTPMoney` (валюта депо), закрывается вся сеточная корзина одним проходом. Удобно для «довести цепочку до +X $ и выйти». По умолчанию `0` (выкл).

Идентификация позиций сетки строгая: `Magic == InpMagic` **И** комментарий содержит подстроку `"GRID-"` (это покрывает и стартовые `GRID-1-B/S` из `PlaceUnconditionalGrid`, и реплейс-уровни `GRID-R1-B/S` из `ReplenishGrid`). Ручные позиции, усреднители (`AVG-`, `PR-AVG`), лок-позиции (`PR-LOCK`, `MANUAL-LOCK`) и любые позиции из других режимов восстановления **не трогаются**.

Параметры группы «Безусловная сетка»:

| Параметр | Описание | Дефолт |
|---|---|---|
| `InpGridCommonTPCount` | Кол-во сработавших ордеров сетки для активации общего ТП (`0`=выкл) | `5` |
| `InpGridCommonTPPts` | Общий ТП от средневзв. цены сетки в пунктах, **на сторону** (`0`=выкл) | `50` |
| `InpGridCommonTPMoney` | Общий ТП по сумме PnL всей сетки в валюте депо (`0`=выкл) | `0.0` |

Денежный триггер имеет приоритет: если PnL ≥ `InpGridCommonTPMoney`, закрывается **вся сетка** независимо от цены. Если денежный триггер не сработал — независимо проверяются ценовые ТП по сторонам.

**Версия 1.44** — Mode 5 (Partial Recovery): закрытие старой сетки усреднителей по профиту при смене тренда.

До 1.43 при флипе тренд-фильтра советник сбрасывал счётчик усреднителей `g_prAvgCount*` на новой стороне (чтобы новая цепочка стартовала немедленно), но **старые** `PR-AVG-*` позиции на «уходящей» стороне оставались висеть и каждая ждала своего личного `InpAvgTPpts`. Пока они ждали, цена уже шла в новую сторону, и старая цепочка садилась в ещё больший минус.

Теперь в момент флипа советник «армирует» сторону, которая стала старой:

- На каждом тике суммирует PnL (`Profit + Swap + Commission`) всех `PR-AVG-B/-S` на этой стороне.
- Как только сумма пересекает порог `InpOldGridCloseProfit` (по умолчанию 0 USD — то есть закрытие при первом же выходе корзины старой сетки в безубыток/плюс), все старые усреднители закрываются разом одной корзиной.
- Если до закрытия тренд флипает ещё раз — сторона переармируется на новую «старую» (направление, которое цена только что бросила).
- Лок-нога (`PR-LOCK`) и исходные убыточные позиции **не трогаются**, флушится только сетка усреднителей.

Управляется новыми ключами:
- `InpCloseOldGridOnTrendFlip` (bool, по умолчанию `true`) — включить флуш старой сетки.
- `InpOldGridCloseProfit` (double, по умолчанию `0.0`) — минимальный суммарный PnL старой сетки для её закрытия (валюта депо, `>=0`).

Состояние (`g_prCloseOldActive`, `g_prCloseOldSide`) сохраняется в `GlobalVariables` (`PR_COA` / `PR_COS`) и сбрасывается на смене режима / кнопкой **Reset State**. Прежнее поведение v1.43 (рестарт счётчиков на новой стороне) сохранено и работает независимо через `InpRestartGridOnTrendFlip` — оба переключателя можно включать вместе или по отдельности.

**Версия 1.43** — фиксы режима 5 (Partial Recovery):

- **Net-positive guard на партиал-закрытии.** Раньше при срабатывании TP усреднителя (PR-AVG) советник *безусловно* отрезал `InpPartCloseLot` от худшей убыточной позиции. Если убыточник был глубоко в минусе, отрезаемый кусок реализовывал убыток, превышающий профит усреднителя — итоговый цикл «закрыть TP + откусить часть» уходил в минус, баланс падал. Теперь чип рассчитывается динамически: размер откусываемого лота подбирается так, чтобы сумма (профит усреднителя + реализованный убыток на отрезанной части) была **не меньше `InpMinNetProfit`** (по умолчанию 0). Если даже минимальный лот брокера не помещается в этот бюджет — чип пропускается, и усреднитель просто фиксирует свой профит. Управляется ключом **`InpEnsureNetPositive`** (по умолчанию `true`); поставив `false`, можно вернуть старое поведение.
- **Перезапуск сетки на смене тренда.** При включённом `InpUseTrendFilter` тренд-фильтр блокирует усреднители на «контртрендовой» стороне. Если тренд (быстрая/медленная MA на старшем ТФ) переворачивается, советник теперь сбрасывает счётчик усреднителей `g_prAvgCount*` и one-per-bar-gate **на новой стороне**, чтобы новая цепочка открылась немедленно: мультипликатор объёма и шага начинается с нуля, спейсинг считается от нуля. Старые усреднители на «старой» стороне продолжают жить и закроются по TP / overlap. Управляется **`InpRestartGridOnTrendFlip`** (по умолчанию `true`).

**Версия 1.42** — фикс: **кнопки панели (BUY/SELL manual, Close All и т.д.) теперь надёжно работают в Strategy Tester (Visual mode)**. В MT5 визуальном тестере событие `CHARTEVENT_OBJECT_CLICK` доставляется в `OnChartEvent` ненадёжно (зависит от билда терминала) — кнопка визуально нажималась, но обработчик не вызывался, ошибок при этом не было. Теперь в `OnTick()` опрашивается `OBJPROP_STATE` всех кнопок, и если кнопка «нажата», её обработчик вызывается принудительно. В live-режиме поведение не меняется (опрос активен только при `MQL_TESTER=true`).

**Версия 1.31** — добавлено: **кнопки ручного открытия BUY/SELL** на панели (для тестирования стратегии в визуальном режиме тестера и на живом графике). Все `input`-параметры переведены на русский — теперь надписи в окне настроек MT5 на русском.

**Версия 1.30** — добавлено: **авто-распил («раскулачивание») лока** в режиме HedgeLock с фазами IDLE → LOCKED → UNWOUND → RELOCKED, переоткрытие частичного встречного лока на провалах, валидация входов, фикс ложного `g_gridPlaced`.

**Версия 1.20** — добавлены: виртуальный TSL по каждой позиции, уведомления Alert/Sound/Push, сохранение состояния между перезапусками, безусловная сетка лимитников.

**Версия 1.10** — добавлены: закрытие по стороне, виртуальные TP/SL/безубыток, кнопки на панели, фильтры по времени и экономкалендарю.

## Установка

1. Скопируйте `RECOVERI.mq5` в `<MT5>/MQL5/Experts/`.
2. В MetaEditor — **Compile** (F7).
3. Перетащите советника на график, разрешите автоторговлю.

## Стратегии восстановления (`InpMode`)

| Режим | Описание |
|------|----------|
| `0 TargetProfit` | Просто ждёт цели и закрывает корзину. Сам не торгует. |
| `1 Averaging` | Усреднение в сторону убытка (одинаковый или линейно растущий лот). |
| `2 MartingaleGrid` | Мартингейл-сетка с множителем лота. |
| `3 HedgeLock` | Локирование чистого объёма встречной позицией. |
| `4 SmartClose` | Парное закрытие: лучшая прибыльная + худшая убыточная. |

## Что брать в управление

- `InpManageScope`: `ALL` / `MANUAL` (magic==0) / `OWN` (по `InpMagic`)
- `InpSymbolScope`: только текущий символ или все символы счёта

## Корзина и цель

- `InpBasketMode`:
  - `COMBINED` — единая корзина BUY+SELL, цель и трейлинг общие.
  - `PER_SIDE` — **раздельные** корзины BUY и SELL: каждая закрывается независимо по своей цели и трейлингу.
- `InpTargetType` — `MONEY` / `PERCENT` от баланса / `PIPS` (эквивалент в пунктах).
- `InpTargetProfit` — значение цели.
- `InpUseBasketTSL` + `InpBasketTSLStart` + `InpBasketTSLStep` — трейлинг корзины (общий или раздельный по сторонам).

## Виртуальные TP/SL/Breakeven по каждой позиции

В отличие от обычных SL/TP, эти **не выставляются на сервер** — брокер их не видит, советник закрывает позиции сам по достижении уровня:

- `InpUseVirtualTP` + `InpVirtualTPPts` — закрытие при +N пунктов.
- `InpUseVirtualSL` + `InpVirtualSLPts` — закрытие при −N пунктов.
- `InpUseVirtualBE` + `InpVirtualBEPts` — после прохождения +N пунктов фиксируется виртуальный безубыток: при возврате в 0 позиция закрывается.

Работают одновременно для всех управляемых позиций (свои/ручные).

## Виртуальный трейлинг-стоп по каждой позиции

`InpUseVirtualTSL` — после того как позиция набрала `InpVirtualTSLStartPts` пунктов прибыли, советник запоминает пик и закрывает позицию, если профит просел на `InpVirtualTSLDistPts` пунктов от пика. Тоже виртуальный — на сервере SL не выставляется.

Работает параллельно с обычными vTP/vSL/vBE и независимо для каждого тикета.

## Уведомления

`InpUseAlert` (всплывающее окно), `InpUseSound` + `InpSoundFile`, `InpUsePush` (Push на телефон через Tools → Options → Notifications). Срабатывают на: vTP/vSL/vBE/vTSL, цель корзины, трейлинг корзины, EmergencyStop.

## Сохранение состояния (Persistence)

`InpUsePersistence` — состояние пишется в `GlobalVariables` под префиксом `RECOVERI_<symbol>_<magic>_` и переживает перезапуск терминала / реинициализацию советника:
- флаг **Paused** и **EmergencyStop**;
- пики корзины (общий и раздельные BUY/SELL);
- **виртуальный безубыток** по каждой позиции (тикеты, у которых уже сработал триггер BE);
- **пики виртуального TSL** по каждой позиции;
- факт того, что **сетка уже была выставлена** (чтобы не переоткрывать после рестарта).

При выгрузке советника (`OnDeinit`) состояние сохраняется. При запуске (`OnInit`) — загружается. Ключи привязаны к символу + magic, поэтому можно запускать несколько копий советника на разных парах одновременно.

## Безусловная сетка лимитников

`InpUseUncondGrid` — при первом запуске сразу выставляет N уровней лимитников (BUY_LIMIT ниже рынка / SELL_LIMIT выше — «фейд»-сетка для восстановления):
- `InpGridSide`: `BOTH` / `BUY` / `SELL` (только одна сторона).
- `InpGridLevels` — количество уровней.
- `InpGridStepPoints` — шаг между уровнями (с учётом минимального `STOPS_LEVEL` брокера).
- `InpGridStartLot` + `InpGridLotMultiplier` — лот первого уровня и множитель (1.0 = одинаковые лоты, >1 = мартингейл).
- `InpGridReplaceFilled` — переоткрывать сработавшие уровни на текущем рынке.
- `InpGridCommonTPCount` — кол-во сработавших ордеров для активации общего ТП сетки (по умолчанию `5`, `0`=выкл).
- `InpGridCommonTPPts` — общий ТП от средневзв. цены сетки в пунктах, **на сторону** (BUY и SELL считаются раздельно, по умолчанию `50`, `0`=выкл).
- `InpGridCommonTPMoney` — общий ТП по сумме PnL всей сетки в валюте депо (по умолчанию `0`=выкл; имеет приоритет над ценовым ТП).

Сработавший лимитник превращается в позицию и автоматически попадает под управление советника (закрытие по цели корзины, виртуальные TP/SL/TSL и т.д.).

## Авто-распил лока (HedgeLock + InpAutoUnlock)

В режиме `MODE_HEDGE_LOCK` (`InpMode=3`) при включении `InpAutoUnlock=true` советник управляет локом по конечному автомату с фазами:

```
IDLE  --(loss > InpLockTriggerLoss)-->  LOCKED
LOCKED  --(одна нога в плюс >= InpUnlockProfitUSD или TSL)-->  UNWOUND
UNWOUND  --(оставшаяся нога в минус >= InpRelockTriggerLoss)-->  RELOCKED
RELOCKED --(одна нога в плюс)-->  UNWOUND
любая  --(все позиции закрыты внешне)-->  IDLE
```

**Как это работает по шагам:**

1. **IDLE** — нет лока. Когда совокупный убыток корзины ≥ `InpLockTriggerLoss`, EA открывает встречную позицию на чистый объём × `InpLockLotFactor` и переходит в **LOCKED**.

2. **LOCKED** — обе стороны открыты. Советник параллельно отслеживает прибыль каждой ноги:
   - Если **любая нога** даёт прибыль ≥ `InpUnlockProfitUSD` (в валюте депозита) — закрывает её, переходит в **UNWOUND**.
   - Если включён `InpUseSideTSL`, дополнительно работает трейлинг: запоминает пик профита ноги и закрывает её при откате с пика на `InpUnlockTSLStep` (активация после пика ≥ `InpUnlockTSLStart`).

3. **UNWOUND** — осталась одна сторона. EA продолжает обычное управление корзиной (цель `InpTargetProfit`, виртуальные TP/SL/TSL применяются к выжившим позициям). Если `InpEnableRelock=true` и оставшаяся нога уходит в минус ≥ `InpRelockTriggerLoss`, открывается **частичный** встречный лок объёмом `остаток × InpRelockLotFactor` (например, 0.5 = половина объёма), переход в **RELOCKED**. Счётчик `g_relockCount` ограничен `InpMaxRelocks`.

4. **RELOCKED** — те же правила, что и в LOCKED. Закрытие выгодной (теперь обычно меньшей) ноги возвращает в UNWOUND.

5. **Сброс в IDLE** происходит автоматически, как только все управляемые позиции закрыты (по любой причине: цель корзины, EmergencyStop, ручная кнопка `Close All`).

**Параметры:**

| Параметр | Описание | Дефолт |
|---|---|---|
| `InpAutoUnlock` | Включить автомат | `false` |
| `InpUnlockProfitUSD` | Профит ноги для её закрытия (валюта депо) | `30.0` |
| `InpUseSideTSL` | Трейлинг профита ноги при распиле | `true` |
| `InpUnlockTSLStart` | Пик ноги для активации TSL | `50.0` |
| `InpUnlockTSLStep` | Откат от пика для закрытия | `20.0` |
| `InpEnableRelock` | Переоткрывать частичный встречный лок | `false` |
| `InpRelockTriggerLoss` | Убыток оставшейся ноги для перелока | `80.0` |
| `InpRelockLotFactor` | Доля объёма для перелока (0..1] | `0.5` |
| `InpMaxRelocks` | Максимум последовательных перелоков | `2` |

**Авто-определение существующего лока:** если включить `InpAutoUnlock=true` уже когда на счёте есть BUY и SELL по символу, EA сразу перейдёт в LOCKED и продолжит распил с текущих позиций.

**Что сохраняется в GlobalVariables** (привязано к символу + magic):
- текущая фаза (`LK_PH`)
- какая сторона осталась после распила (`LK_RS`)
- пики профита ноги (`LK_PB`, `LK_PS`)
- счётчик перелоков (`LK_RC`)

После рестарта терминала автомат восстанавливает ту же фазу, в которой был.

**Панель** показывает строку `Unwind: <PHASE> peakB=… peakS=… relocks=N/M` когда `InpMode=3` и `InpAutoUnlock=true`.

**⚠️ Предупреждение по математике:** распил лока **не выигрывает** математически у простого «дождаться отскока» — каждый цикл «закрыть ногу + опционально переоткрыть» теряет дважды на спреде и оставляет одну сторону голой, увеличивая риск стоп-аута. Распил работает только при сильном продолжении тренда после открытия лока + точно угаданном моменте отскока. Используй на свой риск, обязательно с `InpUseEquityStop`.

## Кнопки на графике

| Кнопка | Действие |
|--------|----------|
| **Close All**     | Закрыть все управляемые позиции |
| **Pause / Resume**| Поставить советник на паузу (не открывает новые сделки, но продолжает следить за TP/SL и целью корзины) |
| **Close BUY**     | Закрыть только BUY-сторону |
| **Close SELL**    | Закрыть только SELL-сторону |
| **Lock Now**      | Сразу залочить чистый объём встречной позицией |
| **Reset Stop**    | Сбросить аварийный equity-стоп |
| **BUY (manual)**  | Открыть ручной BUY на `InpManualLot` (попадает под управление советника) |
| **SELL (manual)** | Открыть ручной SELL на `InpManualLot` (попадает под управление советника) |

### Ручное открытие BUY/SELL (тестер и лайв)

Кнопки **BUY (manual)** и **SELL (manual)** открывают рыночную позицию объёмом `InpManualLot` с тем же `InpMagic`, что и у советника. Это значит, что после открытия позиция автоматически **попадает в его корзину** и обрабатывается всеми режимами восстановления (`Averaging`, `Martingale`, `HedgeLock` с авто-распилом, виртуальный TP/SL/TSL, цель корзины и т.д.).

**Параметры:**
- `InpShowManualButtons` — показывать ли ручные кнопки на панели (по умолчанию `true`).
- `InpManualLot` — лот для ручных кнопок (по умолчанию `0.01`). Меняется только перед запуском (или повторным `OnInit`).

**Условия открытия (защиты):**
- `InpSymbolScope = SCOPE_CURRENT` — открытие только по символу графика;
- не активен `EmergencyStop` (иначе сбрось его кнопкой **Reset Stop**);
- спред ≤ `InpMaxSpreadPts` (если задан);
- `InpManualLot > 0` и нормализованный лот валиден для брокера.

При нарушении любого условия кнопка молча отказывает и пишет в Журнал причину (плюс уведомление, если оно включено).

**Сценарий «вручную попасть в просадку и посмотреть, как EA разруливает»:**

1. В тестере включи **Visual mode** (галочка на главной странице тестера).
2. Настрой советника: `InpMode = 1 (Averaging)` или `2 (Martingale)` или `3 (HedgeLock + InpAutoUnlock=true)`, `InpSymbolScope = 0 (CURRENT)`, `InpManageScope = 0 (ALL)` или `2 (OWN)`, цель корзины (например, `InpTargetProfit = 5.0`).
3. `InpShowManualButtons = true`, `InpManualLot = 0.01`.
4. Запусти прогон, подожди прорисовки графика.
5. Жми **BUY (manual)** или **SELL (manual)** в нужный момент. Когда позиция уйдёт в минус — советник начнёт открывать усреднения / мартингейл-уровни / лок и доводить корзину до цели.
6. Можно нажимать кнопки сколько угодно раз — каждый клик открывает новую позицию.

В обычном режиме тестера (без Visual) кнопки не работают, потому что нет графика для кликов. Используй **только Visual mode** для ручного входа.

> **v1.42:** В визуальном тестере кнопки теперь надёжно срабатывают. Если ты на старой версии (≤1.41) видишь, что нажатие BUY/SELL не даёт никакой реакции и в логе пусто — обнови файл до 1.42.

## Фильтр времени

`InpUseTimeFilter`:
- `InpStartHour` / `InpEndHour` — окно работы (если start > end — переход через полночь).
- `InpTradeMon..InpTradeSun` — флаги дней недели.

Фильтр блокирует **только открытие** новых сделок стратегиями. Закрытие, виртуальные SL/TP, цель корзины и аварийный стоп работают всегда.

## Новостной фильтр (экономкалендарь MT5)

`InpUseNewsFilter` — использует **встроенный** календарь MT5 (`CalendarValueHistoryByCurrency`), внешние API не нужны.

- `InpNewsHigh` / `InpNewsMedium` / `InpNewsLow` — по каким уровням важности блокировать.
- `InpNewsMinsBefore` / `InpNewsMinsAfter` — окно до и после события (мин).
- Фильтр срабатывает по **обеим валютам символа** (base + profit). На индексах/металлах — пропускается.

## Защита счёта

- `InpUseEquityStop` + `InpEquityStopPct` — аварийное закрытие, если equity упало ниже % от баланса. Стоп держит состояние до сброса кнопкой **Reset Stop**.
- `InpCloseOnly` — режим «только закрытие».
- `InpMaxTrades`, `InpMaxLot`, `InpMaxSpreadPts` — предохранители.

## Типовые сценарии

**1) «Закрой ручные сделки в плюсе $10 на любом символе»**
```
InpMode = 0 (TargetProfit), InpManageScope = 1 (MANUAL), InpSymbolScope = 1 (ALL)
InpTargetProfit = 10.0, InpCloseOnly = true
```

**2) «Усредни ручную убыточную EURUSD до безубытка, не торгуй на новостях NFP»**
```
EA на графике EURUSD
InpMode = 1 (Averaging), InpManageScope = 1 (MANUAL), InpSymbolScope = 0 (CURRENT)
InpStepPoints = 300, InpStepMultiplier = 1.2, InpStartLot = 0.01, InpMaxTrades = 8
InpTargetProfit = 5.0
InpUseNewsFilter = true, InpNewsHigh = true, InpNewsMinsBefore=30, InpNewsMinsAfter=30
```

**3) «Раздельные BUY и SELL корзины, каждая в +$5, по виртуальному SL −500 пт»**
```
InpBasketMode = 1 (PER_SIDE)
InpTargetProfit = 5.0
InpUseVirtualSL = true, InpVirtualSLPts = 500
```

**4) «Только европейская сессия, мартингейл с трейлингом»**
```
InpMode = 2, InpUseTimeFilter = true, InpStartHour = 9, InpEndHour = 18
InpUseBasketTSL = true, InpBasketTSLStart = 30, InpBasketTSLStep = 10
```

**5) «Залочить просадку и автоматически распилить лок»**
```
InpMode = 3 (HedgeLock), InpManageScope = 1 (MANUAL), InpSymbolScope = 0 (CURRENT)
InpBasketMode = 1 (PER_SIDE)
InpLockTriggerLoss = 50.0
InpLockLotFactor   = 1.0
InpAutoUnlock        = true
InpUnlockProfitUSD   = 30.0
InpUseSideTSL        = true
InpUnlockTSLStart    = 50.0
InpUnlockTSLStep     = 20.0
InpEnableRelock      = true
InpRelockTriggerLoss = 80.0
InpRelockLotFactor   = 0.5
InpMaxRelocks        = 2
InpUseEquityStop = true, InpEquityStopPct = 30.0
```

**6) «Прогнать стратегию усреднения с ручным входом в визуальном тестере»**
```
InpMode = 1 (Averaging), InpManageScope = 2 (OWN), InpSymbolScope = 0 (CURRENT)
InpStartLot = 0.01, InpStepPoints = 300, InpStepMultiplier = 1.2
InpLotAdd = 0.01, InpMaxTrades = 8
InpTargetType = 0 (MONEY), InpTargetProfit = 5.0
InpUseEquityStop = true, InpEquityStopPct = 50.0
InpShowManualButtons = true, InpManualLot = 0.01
```
Запусти тестер в **Visual mode**. На прорисованном графике жми **BUY (manual)** или **SELL (manual)** — советник возьмёт позицию в управление и при просадке начнёт усреднять до цели.

## Информационная панель

Показывает: режим, область управления, тип корзины, кол-во позиций, BUY/SELL объёмы и средневзвешенные цены, P/L каждой стороны и общий, пик(и), цель(и), активные фильтры (PAUSE/TIME/NEWS), статус.

## Дисклеймер

Усреднение, мартингейл и локирование увеличивают риск потери счёта. Сначала тестер стратегий и демо. Обязательно включайте `InpUseEquityStop`. Автор ответственности не несёт.
